name: global_pay_mobile
description: Global Pay - International Money Transfer Platform (Mobile)

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.0.0
  get_storage: ^2.1.0
  intl: ^0.19.0
  flutter_svg: ^2.0.0
  cached_network_image: ^3.3.0
  shimmer: ^3.0.0
  pinput: ^4.0.0
  firebase_core: ^2.24.0
  firebase_auth: ^4.13.0
  firebase_messaging: ^14.6.0
  google_sign_in: ^6.1.0
  apple_sign_in: ^0.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mockito: ^5.4.0

flutter:
  uses-material-design: true
